#include<iostream>
using namespace std;
int main()
{
  int c,s;
  cin>>c>>s;
  if (s==2*c)
    cout<<"circle can be inside a square";
  else
  cout<<"circle cannot be inside a square";
  //Type your code here.
}